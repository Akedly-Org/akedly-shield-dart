name: akedly_shield
description: Client-side PoW solver, Turnstile helper, and hosted passkey ceremony launcher for Akedly Shield V1.2.
version: 1.1.0
repository: https://github.com/Akedly-Org/akedly-shield-dart

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.0
  webview_flutter: ^4.0.0
  flutter_web_auth_2: ^3.0.0

dev_dependencies:
  test: ^1.24.0
